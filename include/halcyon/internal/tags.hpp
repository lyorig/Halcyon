#pragma once

#include <halcyon/types/numeric.hpp>

// tags.hpp:
// For internal use only. This file is required to fix some circular dependencies.

namespace hal
{
    HAL_TAG(as_size);

    HAL_TAG(infinite_loop);

    HAL_TAG(fill);
}